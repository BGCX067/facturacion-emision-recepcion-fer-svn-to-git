<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mx.gob.se.siger2.dao.unidadvariable.UnidadVariableDAO">
    <!--  selects -->
    <select id="consultar" resultMap="ctUnidadVariable">
        SELECT * FROM CTUNIDADVARIABLE
    </select>
    <!-- Fin  selects -->  
   <resultMap type="ctUnidadVariable" id="ctUnidadVariable">
      <id column="LLUNIDADVARIABLE"  property="id"/>
      <result column="DSNOMBRE" property="nombre"/>   
   </resultMap>
    <!-- fin  resultMaps -->
</mapper>